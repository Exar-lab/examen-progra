package com.buses.examen.Progra.sales.domain;

/** Canal de origen a través del cual se realizó la compra. */
public enum CanalCompra {
    /** Compra realizada desde el sitio web. */
    WEB,
    /** Compra realizada desde la aplicación móvil. */
    APP,
    /** Compra realizada en la ventanilla física. */
    BOLETERIA
}
